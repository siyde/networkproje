(Yeni AWS, Nginx, SSL, oyun yapısı ve domain yapılandırmalarına göre düzenlenmiştir.)

a) Sunucuya (AWS EC2) Bağlanma

Anahtar dosyasının bulunduğu klasöre gidip terminal aç:

ssh -i NetworkAnahtar.pem ubuntu@52.59.239.82

Eğer Permission denied alırsan:
chmod 400 NetworkAnahtar.pem


Sonra tekrar bağlanmayı dene.

b) Proje Dosya Yapısı (Güncel)

Sunucuda proje:

/home/ubuntu/networkproje/
│
├── server.py                 # Tüm oyunların FastAPI + WebSocket backend’i
│
└── /static/
       ├── index.html         # Ana sayfa (Arcade UI)
       ├── pictionary/
       │     └── index.html
       ├── codenames/
       │     └── index.html
       ├── ttt/
       │     └── index.html
       └── pixelwar/
             └── index.html


Bu klasör yapısı birebir şu anda çalışan mimaridir.
HTML dosyaları Nginx tarafından servis edilir, WebSocket trafiği /ws/... olarak server.py tarafından yönetilir.

c) Dosya Düzenleme Komutları
server.py düzenleme:
cd ~/networkproje
sudo nano server.py

Ana sayfa:
nano /home/ubuntu/networkproje/static/index.html

Pictionary:
nano /home/ubuntu/networkproje/static/pictionary/index.html

Codenames:
nano /home/ubuntu/networkproje/static/codenames/index.html

Tic-Tac-Toe (ttt):
nano /home/ubuntu/networkproje/static/ttt/index.html

Pixelwar:
nano /home/ubuntu/networkproje/static/pixelwar/index.html

d) Düzenleme Sonrası Yapılması Gerekenler (ÇOK ÖNEMLİ)

Her nano işleminden sonra mutlaka:

Ctrl + O → Enter (kaydet)
Ctrl + X (çık)

🔵 Sadece HTML dosyası değiştirdiysen:

Nginx test + reload:

sudo nginx -t && sudo systemctl reload nginx

🔴 server.py değiştiyse:

Gameverse servisinin yeniden başlatılması gerekir:

sudo systemctl restart gamehub
sudo systemctl status gamehub   # active (running) olmalı

e) Yeni Oyun Eklemek (Örnek: “wordchain”)

Yeni bir oyun klasörü eklemek istersen:

sudo mkdir /var/www/gameverse/wordchain
sudo nano /var/www/gameverse/wordchain/index.html


WebSocket endpoint'i server.py içinde:

@app.websocket("/ws/wordchain")


Bu şekilde tüm oyunlar tek backend içinde toplanır.

f) Sistemin Altyapısı

Bu proje şu mimariyle çalışır:

FastAPI → Python tabanlı HTTP + WebSocket oyun sunucusu

Nginx → Static dosyalar (HTML/CSS/JS) + WebSocket proxy

Systemd service → server.py arka planda sürekli çalışır

AWS EC2 (Ubuntu 22.04) → Sunucu ortamı

DuckDNS + Let’s Encrypt SSL →
HTTPS + WSS WebSocket için gereklidir
(Siten: https://gameverse-online.duckdns.org
)

g) Nginx Domain ve SSL Yapılandırması (Güncel)

Domain:

gameverse-online.duckdns.org


Nginx ayarı:

sudo nano /etc/nginx/sites-available/gameverse


Server block:

server_name gameverse-online.duckdns.org;

root /home/ubuntu/networkproje/static/
index index.html;

location / {
    try_files $uri $uri/ /index.html;
}

location /ws/ {
    proxy_pass http://127.0.0.1:8000/ws/;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}

SSL alma:
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d gameverse-online.duckdns.org

h) AWS / EC2 Bilgileri (Özel değil, öğrenci içindir)

IP adresin:

18.194.45.143


Web adresi:

https://gameverse-online.duckdns.org

i) Kod Görüntüleme (cat komutu)

Bir dosyayı sadece okumak için:

cat /home/ubuntu/networkproje/static/index.html
